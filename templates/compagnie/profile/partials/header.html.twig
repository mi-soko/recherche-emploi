
<header class="container-detail-profile shadow overflow-hidden p-5" >
    <div class="row">
        <div class="col-md-3">
            <div class="detail-profil-image"  style="margin-top: -20px;" >
                <img src="{{ user.picture }}" class="img-fluid  shadow"  alt="">
            </div>
        </div>
        <div class="col-md-6">
            <h1 class="detail-profil-title">{{ user.fullName | capitalize }}</h1>
            <h4 class="detail-profil-sub-title">Entreprise  {{ user.compagnieName | capitalize }}</h4>
            <div class="card-group" >
                <div class="card" >
                    <div class="card-body p-0 bg-blue-dark" >
                        <p class="m-2 detail-profil-card-text text-white" >Nombre d'offre</p>
                        <p class="m-2 detail-profil-card-text fw-bold text-white " >
                          {{ user.offers.count }}
                        </p>
                    </div>
                </div>
                <div class="card" >
                    <div class="card-body p-0  " >
                        <p class="m-2 detail-profil-card-text fw-bold " >Nombre de project</p>
                        <p class="m-2 detail-profil-card-text fw-bold " >0</p>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="container" >

{#                {% if user.id == app.user.id %}#}
{#                    <a href="{{ path('profil_create_cv') }}" class="btn detail-profil-btn col-12 text-white bg-rose-dark" >#}
{#                        {% if  user.id is not null %} Modifier {% else %} Creer {% endif %}cv profil#}
{#                    </a>#}
{#                {% endif %}#}

            </div>
        </div>
    </div>
</header>
